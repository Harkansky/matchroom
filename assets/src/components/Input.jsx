const Input = () => {
  return (
    <fieldset className="fieldset">
      <legend className="fieldset-legend text-gray-500">Email</legend>
      <input type="text" className="input rounded-lg" placeholder="Type here" />
    </fieldset>
  );
};

export default Input;
